A couple of routines for dumping binary data as hex.

The project build assumes a directory structure like this:

src
|
+-apps
|
+-libs
| |
| +-dump
|
+-3rd

*** BUILDING ON WINDOWS ***

make -f Makefile.win32

*** BUILDING ON LINUX ***

make -f Makefile.linux
